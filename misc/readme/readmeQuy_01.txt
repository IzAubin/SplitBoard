to import a consistent style add in head:

include head.pug

plug in to translate  scss into css:
Live Sass Compiler

fonts
To get fonts from googlefonts:
https://fonts.google.com/

1) Select font, in right tab => select 'link' , copy paste into head.pug(view)

2) from right tab copy CSS rules to specify families & paste into scss/_variables.scss at right place!


style Hn

makes changes in scss/_variables.scss at right place!

All the bootstrap is included in the head.pug 
